{
  "name": "2b-tex",
  "version": "1.0.0",
  "private": true,
  "description": "2B Tex Operations System",
  "scripts": {
    "start": "node start.js",
    "check": "npm run check:app && npm run check:server && npm run check:backend && npm run check:start",
    "check:app": "node --check app.js",
    "check:server": "node --check server.js",
    "check:backend": "node --check backend/server.js && node --check backend/calculations.js && node --check backend/tools/import-localstorage.js",
    "check:start": "node --check start.js"
  }
}
